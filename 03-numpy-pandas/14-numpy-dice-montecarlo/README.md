# 14 — Dice & Monte Carlo in NumPy

Reimplementation of projects 10 and 11 with no per-trial Python loops.

## What's here

- `dice_numpy.py` —
- `pi_numpy.py` —
- `baseline.py` —
- `benchmark.py` —

## Results

_(speedup table goes here)_

## What I learned

_(fill in at the end)_